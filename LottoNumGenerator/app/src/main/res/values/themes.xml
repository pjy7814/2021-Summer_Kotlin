<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LottoNumGenerator" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- 결과하면 테마 -->
    <style name="AppTheme.Result" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryResult</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkResult</item>
        <item name="colorAccent">@color/colorAccentResult</item>
    </style>

    <!-- 별자리 입력화면 테마 -->
    <style name="AppTheme.Constellation" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimaryConstellation</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkConstellation</item>
        <item name="colorAccent">@color/colorAccentConstellation</item>
        <!-- 버튼의 컬러 -->
        <item name="colorButtonNormal">@color/colorButtonConstellation</item>
        <!-- 버튼등 텍스트 컬러 -->
        <item name="android:textColor">@android:color/white</item>
    </style>
    <style name="AppTheme.Name" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryName</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkName</item>
        <item name="colorAccent">@color/colorAccentName</item>
        <!-- 버튼의 컬러 -->
        <item name="colorButtonNormal">@color/colorButtonName</item>
        <!-- 버튼등 텍스트 컬러 -->
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Theme.LottoNumGenerator.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.LottoNumGenerator.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.LottoNumGenerator.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>